services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: clearcut
      POSTGRES_USER: clearcut
      POSTGRES_PASSWORD: clearcut-dev-only
    ports:
      - "5432:5432"
    volumes:
      - clearcut-postgres:/var/lib/postgresql/data

volumes:
  clearcut-postgres:
